<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="PiccoloReader.Views.SheetViewerPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:PiccoloReader.Converters"
    xmlns:skia="clr-namespace:SkiaSharp.Views.Maui.Controls;assembly=SkiaSharp.Views.Maui.Controls"
    xmlns:ctViews="clr-namespace:CommunityToolkit.Maui.Views;assembly=CommunityToolkit.Maui"
    x:Name="ThisPage"
    Title="{Binding Title}">

    <ContentPage.ToolbarItems>
        <ToolbarItem x:Name="ToolPanelToggleItem" Clicked="OnToggleToolPanelClicked">
            <ToolbarItem.IconImageSource>
                <FontImageSource Glyph="&#xE3C9;" FontFamily="MaterialOutlined" Size="24" Color="White" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
        <ToolbarItem x:Name="DeactivateToolItem" Clicked="OnDeactivateToolClicked">
            <ToolbarItem.IconImageSource>
                <FontImageSource Glyph="&#xE14C;" FontFamily="MaterialOutlined" Size="24" Color="White" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
        <ToolbarItem x:Name="BookmarksItem" Clicked="OnBookmarksClicked">
            <ToolbarItem.IconImageSource>
                <FontImageSource Glyph="&#xE98B;" FontFamily="MaterialOutlined" Size="24" Color="White" />
            </ToolbarItem.IconImageSource>
        </ToolbarItem>
    </ContentPage.ToolbarItems>

    <ContentPage.Resources>
        <converters:ByteArrayToImageSourceConverter x:Key="ByteArrayToImageSource" />
    </ContentPage.Resources>

    <Grid BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Black}}">

        <Grid
            x:Name="PageContainer"
            HorizontalOptions="Center"
            VerticalOptions="Center">
            <Grid.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnPageContainerTapped" />
                <PinchGestureRecognizer PinchUpdated="OnPinchUpdated" />
                <PanGestureRecognizer PanUpdated="OnPanUpdated" />
            </Grid.GestureRecognizers>

            <Image
                x:Name="PageImage"
                Source="{Binding CurrentPageImageBytes, Converter={StaticResource ByteArrayToImageSource}}"
                Aspect="AspectFit" />

            <skia:SKCanvasView x:Name="AnnotationCanvas" PaintSurface="OnAnnotationCanvasPaintSurface" />

            <Grid
                x:Name="SelectionOverlay"
                IsVisible="False"
                HorizontalOptions="Start"
                VerticalOptions="Start"
                InputTransparent="False">
                <Border
                    x:Name="SelectionBorder"
                    Margin="20"
                    Stroke="{StaticResource Primary}"
                    StrokeThickness="2"
                    BackgroundColor="Transparent">
                    <Border.GestureRecognizers>
                        <PanGestureRecognizer PanUpdated="OnSelectionMovePanUpdated" />
                    </Border.GestureRecognizers>
                </Border>

                <BoxView
                    x:Name="ResizeHandle"
                    WidthRequest="24"
                    HeightRequest="24"
                    CornerRadius="12"
                    Color="{StaticResource Primary}"
                    HorizontalOptions="Start"
                    VerticalOptions="Start">
                    <BoxView.GestureRecognizers>
                        <PanGestureRecognizer PanUpdated="OnSelectionResizePanUpdated" />
                    </BoxView.GestureRecognizers>
                </BoxView>
            </Grid>

            <ctViews:DrawingView
                x:Name="PencilDrawingView"
                IsVisible="False"
                InputTransparent="False"
                BackgroundColor="Transparent"
                ShouldClearOnFinish="True"
                DrawingLineCompleted="OnPencilDrawingLineCompleted" />

            <ctViews:DrawingView
                x:Name="EraserDrawingView"
                IsVisible="False"
                InputTransparent="False"
                BackgroundColor="Transparent"
                ShouldClearOnFinish="True"
                DrawingLineStarted="OnEraserDrawingLineStarted"
                PointDrawn="OnEraserPointDrawn"
                DrawingLineCompleted="OnEraserDrawingLineCompleted"
                DrawingLineCancelled="OnEraserDrawingLineCancelled" />

            <Ellipse
                x:Name="EraserRadiusIndicator"
                IsVisible="False"
                InputTransparent="True"
                Fill="{AppThemeBinding Light={StaticResource Primary}, Dark={StaticResource PrimaryDark}}"
                Opacity="0.3"
                HorizontalOptions="Start"
                VerticalOptions="Start" />
        </Grid>

        <Label
            x:Name="PageIndicatorLabel"
            Text="{Binding PageIndicatorText}"
            VerticalOptions="End"
            HorizontalOptions="Center"
            Margin="0,0,0,16"
            Padding="12,6"
            BackgroundColor="{StaticResource Primary}"
            TextColor="{StaticResource White}">
            <Label.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnPageIndicatorTapped" />
            </Label.GestureRecognizers>
        </Label>

        <ActivityIndicator
            IsRunning="{Binding IsLoading}"
            IsVisible="{Binding IsLoading}"
            HorizontalOptions="Center"
            VerticalOptions="Center" />

        <Border
            x:Name="TrashTarget"
            IsVisible="False"
            WidthRequest="56"
            HeightRequest="56"
            Margin="0,0,0,88"
            HorizontalOptions="Center"
            VerticalOptions="End"
            BackgroundColor="{StaticResource Primary}"
            StrokeThickness="0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="28" />
            </Border.StrokeShape>
            <Border.GestureRecognizers>
                <TapGestureRecognizer Tapped="OnTrashTargetTapped" />
            </Border.GestureRecognizers>
            <Image HorizontalOptions="Center" VerticalOptions="Center">
                <Image.Source>
                    <FontImageSource Glyph="&#xE872;" FontFamily="MaterialOutlined" Size="26" Color="White" />
                </Image.Source>
            </Image>
        </Border>

        <Border
            x:Name="ToolPanel"
            IsVisible="False"
            WidthRequest="260"
            HorizontalOptions="End"
            VerticalOptions="Fill"
            BackgroundColor="{AppThemeBinding Light={StaticResource Gray100}, Dark={StaticResource Gray600}}"
            StrokeThickness="0">
            <Grid RowDefinitions="Auto,*">

                <Grid Grid.Row="0" RowDefinitions="Auto,1">

                    <Grid Grid.Row="0" ColumnDefinitions="*,*,*">
                        <Grid Grid.Column="0" Padding="0,16" BackgroundColor="Transparent">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnToolTabTapped" CommandParameter="MusicIcons" />
                            </Grid.GestureRecognizers>
                            <Image HeightRequest="24" HorizontalOptions="Center">
                                <Image.Source>
                                    <FontImageSource x:Name="MusicIconsTabIcon" Glyph="&#xE405;" FontFamily="MaterialOutlined" Size="22" Color="{StaticResource Primary}" />
                                </Image.Source>
                            </Image>
                        </Grid>

                        <Grid Grid.Column="1" Padding="0,16" BackgroundColor="Transparent">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnToolTabTapped" CommandParameter="Pencil" />
                            </Grid.GestureRecognizers>
                            <Image HeightRequest="24" HorizontalOptions="Center">
                                <Image.Source>
                                    <FontImageSource x:Name="PencilTabIcon" Glyph="&#xE3C9;" FontFamily="MaterialOutlined" Size="22" Color="{StaticResource Gray500}" />
                                </Image.Source>
                            </Image>
                        </Grid>

                        <Grid Grid.Column="2" Padding="0,16" BackgroundColor="Transparent">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnToolTabTapped" CommandParameter="Eraser" />
                            </Grid.GestureRecognizers>
                            <Image HeightRequest="24" HorizontalOptions="Center">
                                <Image.Source>
                                    <FontImageSource x:Name="EraserTabIcon" Glyph="&#xE6D0;" FontFamily="MaterialOutlined" Size="22" Color="{StaticResource Gray500}" />
                                </Image.Source>
                            </Image>
                        </Grid>
                    </Grid>

                    <BoxView Grid.Row="1" HeightRequest="1" HorizontalOptions="Fill" Color="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray500}}" />
                </Grid>

                <ScrollView Grid.Row="1">
                    <Grid Padding="12">

                        <VerticalStackLayout x:Name="MusicIconsSection" Spacing="4">
                            <Label Text="Music Icons" FontAttributes="Bold" FontSize="16" Margin="0,0,0,8" />
                            <CollectionView ItemsSource="{Binding Source={x:Reference ThisPage}, Path=IconCategories}">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <VerticalStackLayout Spacing="4" Margin="0,0,0,12">
                                            <Label Text="{Binding Name}" FontAttributes="Bold" FontSize="14" />
                                            <FlexLayout Wrap="Wrap" Direction="Row" BindableLayout.ItemsSource="{Binding Icons}">
                                                <BindableLayout.ItemTemplate>
                                                    <DataTemplate>
                                                        <Border
                                                            WidthRequest="52"
                                                            HeightRequest="52"
                                                            Margin="4"
                                                            Padding="4"
                                                            BackgroundColor="{StaticResource White}"
                                                            StrokeThickness="1">
                                                            <Border.GestureRecognizers>
                                                                <TapGestureRecognizer
                                                                    Tapped="OnIconPickerTapped"
                                                                    CommandParameter="{Binding Key}" />
                                                            </Border.GestureRecognizers>
                                                            <skia:SKCanvasView
                                                                WidthRequest="40"
                                                                HeightRequest="40"
                                                                HorizontalOptions="Center"
                                                                VerticalOptions="Center"
                                                                PaintSurface="OnIconGlyphPaintSurface" />
                                                        </Border>
                                                    </DataTemplate>
                                                </BindableLayout.ItemTemplate>
                                            </FlexLayout>
                                        </VerticalStackLayout>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>

                        <VerticalStackLayout x:Name="PencilSection" IsVisible="False" Spacing="8">
                            <Label Text="Pencil" FontAttributes="Bold" FontSize="16" />
                            <Label Text="Color" FontSize="14" />
                            <HorizontalStackLayout Spacing="10">
                                <Border
                                    x:Name="ColorRingBlack"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    StrokeThickness="2"
                                    Stroke="Transparent"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20" />
                                    </Border.StrokeShape>
                                    <Border
                                        WidthRequest="28"
                                        HeightRequest="28"
                                        BackgroundColor="Black"
                                        StrokeThickness="0"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="14" />
                                        </Border.StrokeShape>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnPencilColorTapped" CommandParameter="#000000" />
                                        </Border.GestureRecognizers>
                                    </Border>
                                </Border>
                                <Border
                                    x:Name="ColorRingRed"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    StrokeThickness="2"
                                    Stroke="Transparent"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20" />
                                    </Border.StrokeShape>
                                    <Border
                                        WidthRequest="28"
                                        HeightRequest="28"
                                        BackgroundColor="Red"
                                        StrokeThickness="0"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="14" />
                                        </Border.StrokeShape>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnPencilColorTapped" CommandParameter="#FF0000" />
                                        </Border.GestureRecognizers>
                                    </Border>
                                </Border>
                                <Border
                                    x:Name="ColorRingBlue"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    StrokeThickness="2"
                                    Stroke="Transparent"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20" />
                                    </Border.StrokeShape>
                                    <Border
                                        WidthRequest="28"
                                        HeightRequest="28"
                                        BackgroundColor="Blue"
                                        StrokeThickness="0"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="14" />
                                        </Border.StrokeShape>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnPencilColorTapped" CommandParameter="#0000FF" />
                                        </Border.GestureRecognizers>
                                    </Border>
                                </Border>
                                <Border
                                    x:Name="ColorRingGreen"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    StrokeThickness="2"
                                    Stroke="Transparent"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20" />
                                    </Border.StrokeShape>
                                    <Border
                                        WidthRequest="28"
                                        HeightRequest="28"
                                        BackgroundColor="Green"
                                        StrokeThickness="0"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="14" />
                                        </Border.StrokeShape>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnPencilColorTapped" CommandParameter="#008000" />
                                        </Border.GestureRecognizers>
                                    </Border>
                                </Border>
                                <Border
                                    x:Name="ColorRingOrange"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    StrokeThickness="2"
                                    Stroke="Transparent"
                                    BackgroundColor="Transparent"
                                    HorizontalOptions="Center"
                                    VerticalOptions="Center">
                                    <Border.StrokeShape>
                                        <RoundRectangle CornerRadius="20" />
                                    </Border.StrokeShape>
                                    <Border
                                        WidthRequest="28"
                                        HeightRequest="28"
                                        BackgroundColor="Orange"
                                        StrokeThickness="0"
                                        HorizontalOptions="Center"
                                        VerticalOptions="Center">
                                        <Border.StrokeShape>
                                            <RoundRectangle CornerRadius="14" />
                                        </Border.StrokeShape>
                                        <Border.GestureRecognizers>
                                            <TapGestureRecognizer Tapped="OnPencilColorTapped" CommandParameter="#FFA500" />
                                        </Border.GestureRecognizers>
                                    </Border>
                                </Border>
                            </HorizontalStackLayout>
                            <Label Text="Width" FontSize="14" />
                            <skia:SKCanvasView
                                x:Name="PencilWidthPreview"
                                HeightRequest="48"
                                HorizontalOptions="Fill"
                                Margin="0,4"
                                PaintSurface="OnPencilWidthPreviewPaintSurface" />
                            <Slider x:Name="PencilWidthSlider" Minimum="0.003" Maximum="0.02" Value="{Binding PencilStrokeWidth}" ValueChanged="OnPencilWidthChanged" />
                        </VerticalStackLayout>

                        <VerticalStackLayout x:Name="EraserSection" IsVisible="False" Spacing="8">
                            <Label Text="Eraser" FontAttributes="Bold" FontSize="16" />
                            <Label Text="Size" FontSize="14" />
                            <Slider x:Name="EraserRadiusSlider" Minimum="0.02" Maximum="0.08" Value="{Binding EraserRadius}" ValueChanged="OnEraserRadiusChanged" />
                        </VerticalStackLayout>

                    </Grid>
                </ScrollView>
            </Grid>
        </Border>

    </Grid>
</ContentPage>
